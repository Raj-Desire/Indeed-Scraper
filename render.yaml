services:
  - type: web
    name: indeed-job-scraper
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: DASHBOARD_HOST
        value: 0.0.0.0
      - key: SCRAPER_HEADLESS
        value: "true"
